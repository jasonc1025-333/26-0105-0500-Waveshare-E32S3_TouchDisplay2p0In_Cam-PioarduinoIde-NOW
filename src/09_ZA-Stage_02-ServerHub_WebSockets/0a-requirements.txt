# Pure WebSocket Server Requirements
# Install with: pip install -r requirements.txt

# ============================================================================
# VERSION COMPATIBILITY NOTES (26-0125-0900)
# ============================================================================
# HISTORY:
# 1. simple-websocket 1.0.0 (FAILED)
#    - Werkzeug compatibility issues (requires 2.x, Flask 3.x needs 3.x)
#    - 400 Bad Request errors, unreliable
#
#### jwc 26-0125-0900 ARCHIVED: flask-sock (replaced by python-socketio)
# 2. flask-sock 0.6.0 (FAILED - FREEZE ISSUE)
#    - Compatible with Flask 3.x, worked initially
#    - CRITICAL BUG: ws.receive() blocks forever on incomplete messages
#    - No timeout support, freezes at message #20-33
#    - Not production-ready!
#
# 3. python-socketio (CURRENT - RECOMMENDED)
#    - Industry standard, 10× more popular than flask-sock
#    - Built-in timeout handling (solves freeze issue!)
#    - Excellent error recovery, auto-reconnect
#    - Production-ready, used by Fortune 500 companies
#    - Active development (2024+)
# ============================================================================

# Core dependencies - python-socketio stack
Flask==3.0.0                    # Latest stable Flask
Flask-CORS==4.0.0               # CORS support for cross-origin requests
python-socketio==5.11.0         # Robust WebSocket/Socket.IO implementation
python-engineio==4.9.0          # Engine.IO protocol (required by python-socketio)
eventlet==0.35.2                # Async networking library (recommended for python-socketio)

#### jwc 26-0125-0900 ARCHIVED: flask-sock (replaced by python-socketio)
#### Flask==3.0.0          # Latest stable Flask (no need to downgrade!)
#### Flask-CORS==4.0.0     # CORS support for cross-origin requests
flask-sock==0.6.0     # Reliable WebSocket implementation (works with Flask 3.x!)
